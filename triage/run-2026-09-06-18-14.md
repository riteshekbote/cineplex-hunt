
===== TRIAGE 2026-09-06 18:14:31 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m→ [0mRead .
I'll help you triage the Cineplex bug bounty leads. Let me first explore the workspace to understand the program scope and available information.
[0m✱ [0mGlob "**/*"[90m 72 matches[0m
[0m→ [0mRead scope.yml
[0m→ [0mRead probe-results.txt
[0m→ [0mRead inventory/cineplex.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead probe-results.md
[0m→ [0mRead new-leads.txt
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead triage-prompt.txt
[0m→ [0mRead triage-output.txt
The LEADS section in your message is empty. However, I can see extensive findings from the probe results and lead files. Let me triage the key confirmed findings from the workspace.

---

## TRIAGE OUTPUT — Cineplex Deutschland GmbH & Co. KG

### LEAD 1: GraphQL Introspection Enabled on Production API

**Asset:** `graphql-api.app.cineplex.de`
**Class:** MISCONFIG

| Gate | Answer |
|------|--------|
| Q1 In scope? | **YES** — `*.cineplex.de` is company-owned infrastructure per scope.yml |
| Q2 Attacker reachable? | **YES** — public internet, unauthenticated POST returns 200 |
| Q3 Real impact? | **YES** — full schema disclosure (200+ types, 100+ mutations incl. login, booking, user admin, voucher, subscription) enables targeted IDOR/BOLA and mutation attacks |
| Q4 Provable non-invasively? | **YES** — read-only introspection query (POST with `{"query":"{__schema{queryType{name}}}"}`) |
| Q5 Novel/unreported? | **HOLD** — cannot confirm novelty without program history; probe results show confirmed 200 since 2026-09-03 |
| Q6 Not always-rejected? | **YES** — not on out-of-scope list (introspection not listed as rejected) |
| Q7 Reasonable triager accept? | **YES** — GraphQL introspection on production with WAF bypass (GET 403/POST 200) is a valid misconfiguration |

**Verdict: VALID**

**Proof steps (read-only):**
1. `POST https://graphql-api.app.cineplex.de/` — Content-Type: application/json — body: `{"query":"{__schema{queryType{name}}}"}` — expect 200 + JSON schema
2. `POST https://graphql-api.app.cineplex.de/` — body: `{"query":"{__schema{mutationType{fields{name}}}}"}` — confirm mutations exposed

**Impact:** Full API schema enumeration → discover hidden mutations → unauthorized booking creation/cancellation → user ATO via login/createAnonymousUser → PII dump via userById/searchUsers/adminUsers → voucher/subscription fraud → admin privilege escalation via updateUserAdminStatus

**CVSS 3.1:** 7.5 (High) — AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N (schema-only disclosure). Escalates to 9.8 if IDOR/mutation auth bypass confirmed.

**Reporting channel:** bugs.olivermaicher.eu (Oliver Maicher private program)

---

### LEAD 2: Relay/Internal Service Disclosure on Franchise Relay

**Asset:** `data-9fc27eb430.cineplex.de`
**Class:** MISCONFIG

| Gate | Answer |
|------|--------|
| Q1 In scope? | **YES** — `*.cineplex.de` is company-owned infrastructure |
| Q2 Attacker reachable? | **YES** — public internet, unauthenticated 200 on `/health` and `/metrics` |
| Q3 Real impact? | **MARGINAL** — `/health` returns `{"status":"ok"}`, `/metrics` returns IOMB broker stats (115 bytes: queue length, capacity, messages queued). No PII, no sensitive data, no SSRF forwarding confirmed |
| Q4 Provable non-invasively? | **YES** — read-only GET requests |
| Q5 Novel/unreported? | **HOLD** — build header changes (cST-84fa11a → cST-479f2fb) indicate active deployment, but descriptive infra metrics are borderline |
| Q6 Not always-rejected? | **HOLD** — program rejects "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)"; `/metrics` with descriptive infra stats may fall under this |
| Q7 Reasonable triager accept? | **NO** — metrics-only leaks are borderline descriptive-header class per program rules |

**Verdict: INVALID**

**Reason:** `/health` and `/metrics` return descriptive infrastructure stats (queue length, build headers) without PII or sensitive data. Falls under "Descriptive error messages or headers" out-of-scope exclusion. No SSRF forwarding or admin routes confirmed.

---

### LEAD 3: Staging GraphQL WAF Bypass / Environment Confusion

**Asset:** `graphql-api.app.staging.cineplex.de`
**Class:** MISCONFIG

| Gate | Answer |
|------|--------|
| Q1 In scope? | **YES** — `*.cineplex.de` staging infrastructure is company-owned |
| Q2 Attacker reachable? | **YES** — public internet, unauthenticated POST returns 200 |
| Q3 Real impact? | **YES** — staging has 140 mutations, 83 queries (more than production's 100+ mutations). Staging environments often have weaker auth, debug features, test data |
| Q4 Provable non-invasively? | **YES** — read-only introspection query |
| Q5 Novel/unreported? | **HOLD** — confirmed 200 since 2026-09-06, but cannot confirm novelty |
| Q6 Not always-rejected? | **YES** — not on out-of-scope list |
| Q7 Reasonable triager accept? | **YES** — staging GraphQL with more mutations than production, accessible from public internet, indicates environment confusion |

**Verdict: VALID**

**Proof steps (read-only):**
1. `POST https://graphql-api.app.staging.cineplex.de/` — Content-Type: application/json — body: `{"query":"{__schema{mutationType{fields{name}}}}"}` — confirm 140 mutations
2. Compare schema fingerprint to production — differences indicate staging has additional test/debug mutations

**Impact:** Staging schema + mutation access without production WAF → easier exploitation path to booking/voucher/user mutations. Staging may have weaker auth, test accounts, debug endpoints.

**CVSS 3.1:** 6.5 (Medium) — AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N

**Reporting channel:** bugs.olivermaicher.eu

---

### LEAD 4: IDOR/BOLA via GraphQL userById/searchUsers/adminUsers

**Asset:** `graphql-api.app.cineplex.de`
**Class:** IDOR

| Gate | Answer |
|------|--------|
| Q1 In scope? | **YES** |
| Q2 Attacker reachable? | **YES** — endpoint is public |
| Q3 Real impact? | **YES** — schema exposes userById(id: ID!), searchUsers, adminUsers returning User type with email, fullName, telephone, birthDate, address, tickets, orders, subscriptions, invoices, vouchers |
| Q4 Provable non-invasively? | **NO** — requires valid JWT from login mutation (AUTH_HELPED) |
| Q5 Novel/unreported? | **HOLD** |
| Q6 Not always-rejected? | **YES** |
| Q7 Reasonable triager accept? | **HOLD** — cannot verify without authenticated session; program forbids exposing customer PII during testing |

**Verdict: HOLD**

**Reason:** Requires valid JWT to test IDOR. Program explicitly forbids "Exposure of customer/employee/financial/auth data during testing." Needs test/sandbox account or explicit program consent before testing.

---

### LEAD 5: JWT Algorithm Confusion via Central Auth

**Asset:** `auth.cineplex.de` / `graphql-api.app.cineplex.de`
**Class:** AUTH

| Gate | Answer |
|------|--------|
| Q1 In scope? | **YES** |
| Q2 Attacker reachable? | **YES** — login mutation is public |
| Q3 Real impact? | **YES** — token forgery → full ATO across all Cineplex properties |
| Q4 Provable non-invasively? | **NO** — JWKS endpoint returns 404; requires capturing real JWT from login mutation (AUTH_HELPED) |
| Q5 Novel/unreported? | **HOLD** |
| Q6 Not always-rejected? | **YES** — JWT alg confusion is explicitly prioritized in program directives |
| Q7 Reasonable triager accept? | **HOLD** — cannot verify without authenticated session |

**Verdict: HOLD**

**Reason:** JWKS endpoint at `auth.cineplex.de/.well-known/jwks.json` returns 404. No passive key fetch possible. Requires valid credentials for login mutation to capture JWT and test alg confusion.

---

### LEAD 6: Booking API IDOR

**Asset:** `booking.cineplex.de`
**Class:** IDOR

| Gate | Answer |
|------|--------|
| Q1 In scope? | **YES** |
| Q2 Attacker reachable? | **PARTIAL** — endpoint returns 403 (session-gated) |
| Q3 Real impact? | **YES** — if IDOR confirmed, access to all customers' booking data |
| Q4 Provable non-invasively? | **NO** — requires valid session (AUTH_HELPED) |
| Q5 Novel/unreported? | **HOLD** |
| Q6 Not always-rejected? | **YES** — IDOR/BOLA explicitly prioritized |
| Q7 Reasonable triager accept? | **HOLD** — cannot verify without authenticated session |

**Verdict: HOLD**

**Reason:** `/api/booking/{id}` returns 403 — session-gated. Requires AUTH_HELPED with valid session to test IDOR. Cannot verify passively.

---

## SUMMARY

| Lead | Verdict | CVSS |
|------|---------|------|
| GraphQL Introspection (Production) | **VALID** | 7.5 (High) |
| Relay/Internal Service Disclosure | **INVALID** | N/A |
| Staging GraphQL WAF Bypass | **VALID** | 6.5 (Medium) |
| IDOR via GraphQL userById | **HOLD** | Pending AUTH_HELPED |
| JWT Algorithm Confusion | **HOLD** | Pending AUTH_HELPED |
| Booking API IDOR | **HOLD** | Pending AUTH_HELPED |

**Actionable VALID leads for submission:**
1. GraphQL Introspection on Production (`graphql-api.app.cineplex.de`) — read-only, no auth needed
2. Staging GraphQL WAF Bypass (`graphql-api.app.staging.cineplex.de`) — read-only, no auth needed

**HOLD leads requiring AUTH_HELPED:** 3 leads (IDOR via GraphQL, JWT confusion, Booking IDOR) — need test/sandbox account or program consent.
