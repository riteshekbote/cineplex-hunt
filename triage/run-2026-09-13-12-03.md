
===== TRIAGE 2026-09-13 12:03:51 UTC =====
[0m
> build · mimo-v2.5-free
[0m
I'll check the workspace to locate the probe results and program scope information.
[0m✱ [0mGlob "**/*.yml"[90m 1 match[0m
[0m✱ [0mGlob "**/leads*"[90m 0 matches[0m
[0m✱ [0mGlob "**/*.md"[90m 91 matches[0m
[0m→ [0mRead probe-results.md
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/cineplex.md
[0m→ [0mRead inventory/cineplex.md[90m [offset=566][0m
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-laguna.md
Let me read the remaining content and the individual lead files.
[0m→ [0mRead triage/run-2026-09-13-06-17.md
[0m→ [0mRead leads/reposcan-cineplex.md
Let me also check the most recent triage run and the reposcan lead for additional context.
[0m→ [0mRead reports/valid-bugs.md
[0m→ [0mRead recon-notes/DEDICATED-DEEP.md[90m [limit=200][0m
Now I have all the data. Let me produce the triage. The "LEADS" section in the user prompt is empty, but the hunt bot has accumulated many hypotheses across the lead files and inventory KB. I'll triage the distinct leads that emerged from the bot's probe campaigns.

---

# CINEPLEX TRIAGE — 2026-09-13

## Lead 1: Production GraphQL Introspection (schema disclosure + WAF bypass)

**Asset:** `graphql-api.app.cineplex.de`
**Class:** MISCONFIG
**Source:** bigpickle (conf 70→confirmed), nemotron3 (conf 95), inventory KB (2026-09-03 23:48+)

| Gate | Answer |
|---|---|
| Q1 In scope? | YES — `graphql-api.app.cineplex.de` is `*.cineplex.de`; program covers "all APIs, customer portals, digital systems" |
| Q2 Reachable? | YES — public internet, unauth; POST bypasses Cloudflare WAF (GET 403 / POST 200) |
| Q3 Real impact? | YES — full schema disclosure exposes 100+ mutations (login, booking, user admin, voucher, subscription) and PII-bearing queries (userById, searchUsers, adminUsers) |
| Q4 Provable non-invasively? | YES — single read-only POST `{"query":"{__schema{queryType{name}}}"}` returns 200 + JSON schema; no data modified, no PII fetched |
| Q5 Novel/unreported? | YES — prior triages at 2026-09-06/07/10/11/12 all marked VALID; not on any rejected list |
| Q6 Not always-rejected? | YES — introspection is not on the out-of-scope list; program explicitly accepts it |
| Q7 Reasonable triager? | YES — production GraphQL introspection enabled behind a bypassable WAF rule is a standard accepted finding |

**Verdict: VALID**

**Proof:** POST `https://graphql-api.app.cineplex.de/` with body `{"query":"{__schema{queryType{name fields{name args{name type{name}}}}}"}` — 200 + full schema JSON.
**Impact:** Full API surface enumeration → attacker learns all resolvers, input types, and mutation signatures → enables targeted IDOR/BOLA/privilege-escalation probes.
**CVSS 3.1:** 5.3 (Medium) — Confidentiality impact; no data exfiltration from introspection alone.
**Report channel:** bugs.olivermaicher.eu — include POST request/response, note GET=403/POST=200 WAF bypass.

---

## Lead 2: Staging GraphQL Introspection + Environment Confusion

**Asset:** `graphql-api.app.staging.cineplex.de`
**Class:** MISCONFIG
**Source:** bigpickle (conf 55→confirmed), nemotron3 (conf 7.2→confirmed), inventory KB

| Gate | Answer |
|---|---|
| Q1 In scope? | YES — `*.staging.cineplex.de` is company infrastructure; program covers all infrastructure |
| Q2 Reachable? | YES — public internet, unauth; same WAF bypass pattern as prod (GET 403 / POST 200) |
| Q3 Real impact? | YES — staging mirrors prod schema (140 mutations, 83 queries); environment confusion = easier exploitation path; staging additionally exposes Spring Data JPA REST endpoints (userPasswordResets, userRegistrations) via introspection |
| Q4 Provable non-invasively? | YES — single read-only POST introspection query returns 200 + full schema |
| Q5 Novel/unreported? | YES — distinct from Lead 1 (separate env, separate host, additional staging-only artifacts) |
| Q6 Not always-rejected? | YES |
| Q7 Reasonable triager? | YES — staging env exposed to public internet with identical prod schema is a valid finding |

**Verdict: VALID**

**Proof:** POST `https://graphql-api.app.staging.cineplex.de/` with body `{"query":"{__schema{mutationType{fields{name}}}"}` — 200 + mutation list including staging-only `testing_getConfirmationCode`.
**Impact:** Staging environment confusion → easier auth-bypass path; staging-specific mutations may lack production auth gates.
**CVSS 3.1:** 6.5 (Medium) — broader attack surface than prod introspection alone.
**Report channel:** bugs.olivermaicher.eu — bundle with Lead 1 as related finding.

---

## Lead 3: Structural IDOR — GraphQL userById/invoice/order/ticket Auth-Omission

**Asset:** `graphql-api.app.cineplex.de`
**Class:** IDOR
**Source:** bigpickle (conf 62), nemotron3 (conf 85), inventory KB (confirmed 2026-09-10+)

| Gate | Answer |
|---|---|
| Q1 In scope? | YES — production GraphQL API |
| Q2 Reachable? | YES — public, unauth; GET-based query execution confirmed |
| Q3 Real impact? | YES — if confirmed with valid data, cross-user PII disclosure (email, phone, address, tickets, orders, subscriptions, invoices) |
| Q4 Provable non-invasively? | PARTIAL — `userById(id:"0")` → 200 `INVALID_ID` with `decodePublicId` stacktrace (typed error) vs `currentUser` → 200 `UNAUTHENTICATED` (auth gate fires). This proves 4/4 resolvers process IDs without auth context. However: no real user record was returned. Program forbids exposing customer PII during testing. |
| Q5 Novel/unreported? | Structural proof is novel; full exploitation not yet attempted |
| Q6 Not always-rejected? | YES — IDOR is explicitly prioritized |
| Q7 Reasonable triager? | HOLD — structural proof is strong (control-complete: 4 auth-omission resolvers vs 4 firing-gate resolvers), but strict triager will demand at least one valid cross-user PII fetch or test-account demonstration |

**Verdict: HOLD**

**Reason:** Auth-omission structural proof is convincing but no real PII was exfiltrated. Program rule forbids exposing customer data during testing. Needs test/sandbox account or explicit program consent for two-account proof.
**Proof (current):** `?query=%7BuserById(id%3A%220%22)%7Bid%7D%7D` → 200 `INVALID_ID` (no Authorization header); `?query=%7BcurrentUser%7Bid%7D%7D` → 200 `UNAUTHENTICATED` on same GET surface.
**Impact if confirmed:** Critical — full PII dump of all Cineplex users.
**CVSS 3.1:** Not assigned (HOLD — unconfirmed exploitation).
**Next step:** Request test credentials from program; or wait for AUTH_HELPED session.

---

## Lead 4: Staging testing_getConfirmationCode Auth Bypass

**Asset:** `graphql-api.app.staging.cineplex.de`
**Class:** AUTH
**Source:** bigpickle, inventory KB (2026-09-06 22:19+)

| Gate | Answer |
|---|---|
| Q1 In scope? | YES — staging infrastructure |
| Q2 Reachable? | YES — resolves with zero auth, backend hit (405-method-mismatch response) |
| Q3 Real impact? | LOW — `testing_getConfirmationCode` resolves authlessly but returns 405 (method mismatch on internal Spring Data JPA endpoint); no actual confirmation code or sensitive data returned |
| Q4 Provable non-invasively? | YES — single GET; no data modified |
| Q5 Novel/unreported? | YES — staging-only mutation; prod correctly gates with FORBIDDEN |
| Q6 Not always-rejected? | YES |
| Q7 Reasonable triager? | BORDERLINE — missing environment guard is real, but the endpoint returns 405 (method mismatch), not actionable data; triager may classify as informational |

**Verdict: HOLD**

**Reason:** Missing env guard is confirmed, but the endpoint returns 405 (method mismatch) not a confirmation code. Strict triager may reject as low/no-impact informational. Report as part of staging env confusion (Lead 2) rather than standalone.
**Proof:** `https://graphql-api.app.staging.cineplex.de/userPasswordResets` → 405 backend hit vs `https://graphql-api.app.cineplex.de/userPasswordResets` → 403 FORBIDDEN.
**Impact if escalated:** Test-account takeover via confirmation code interception.
**CVSS 3.1:** Not assigned (informational — 405 response only).

---

## Lead 5: Relay Health/Metrics Disclosure (data-9fc27eb430.cineplex.de)

**Asset:** `data-9fc27eb430.cineplex.de`
**Class:** MISCONFIG
**Source:** bigpickle (conf 60→68), nemotron3 (conf 7.5)

| Gate | Answer |
|---|---|
| Q1 In scope? | YES — `*.cineplex.de` franchise relay |
| Q2 Reachable? | YES — public, unauth; `/health` and `/metrics` return 200 |
| Q3 Real impact? | NO — `/health` returns `{"status":"ok"}` (15B); `/metrics` returns IOMB broker stats only (mode, queue_length=0, queue_capacity=30000, messages_queued ~553M, messages_dropped=0). No PII, no secrets, no internal hostnames. Purely descriptive infrastructure metrics. |
| Q4 Provable non-invasively? | YES — GET requests only |
| Q5 Novel/unreported? | Repeatedly probed since 2026-09-04; no new surface found |
| Q6 Not always-rejected? | Program rejects "descriptive error messages or headers" and "exposure of known public files"; relay metrics-only is borderline descriptive-header class |
| Q7 Reasonable triager? | NO — 115 bytes of broker stats with no sensitive data is informational at best; triager will reject as descriptive-infrastructure-only |

**Verdict: INVALID**

**Reason:** 115 bytes of IOMB broker queue stats (queue_length, queue_capacity, messages_queued, messages_dropped) with no PII, no secrets, no internal topology. Falls under "descriptive error messages/headers" out-of-scope class. No exploitable security impact.
**Additional:** Common paths `/config`, `/info`, `/env`, `/status`, `/debug`, `/routes`, `/api`, `/api/`, `/admin`, `/proxy` all returned 404. No undocumented API surface or SSRF forwarding confirmed. Speculative SSRF hypothesis remains unproven after 10+ probe cycles.

---

## Lead 6: JWT Algorithm Confusion (auth.cineplex.de / graphql-api.app.cineplex.de)

**Asset:** auth.cineplex.de / graphql-api.app.cineplex.de
**Class:** AUTH
**Source:** bigpickle (conf 45→50), nemotron3 (conf 55→70)

| Gate | Answer |
|---|---|
| Q1 In scope? | YES — central auth + GraphQL API |
| Q2 Reachable? | PARTIAL — GraphQL host is reachable; JWKS endpoint returns 404 (no passive key fetch); login mutation exists but requires valid credentials |
| Q3 Real impact? | CRITICAL if confirmed — token forgery → full ATO across all Cineplex properties |
| Q4 Provable non-invasively? | NO — requires: (1) valid credentials to trigger login mutation and capture JWT, (2) decode JWT header to confirm RS256, (3) forge HS256 with public key, (4) replay to protected endpoint. Steps 1-4 all require AUTH_HELPED. JWKS 404 blocks passive key acquisition. |
| Q5 Novel/unreported? | Hypothesis is sound but unproven |
| Q6 Not always-rejected? | YES — JWT alg confusion is explicitly prioritized |
| Q7 Reasonable triager? | HOLD — high potential impact but zero proof; triager will reject hypothesis-only submission |

**Verdict: HOLD**

**Reason:** JWKS endpoint 404 blocks passive verification. Requires valid test credentials (AUTH_HELPED) for login mutation → JWT capture → algorithm analysis → forgery test. No proof possible without account.
**Next step:** Request test credentials from program; defer until AUTH_HELPED session available.

---

## Summary

| # | Lead | Verdict | CVSS | Action |
|---|---|---|---|---|
| 1 | Prod GraphQL Introspection | **VALID** | 5.3 | Report to bugs.olivermaicher.eu |
| 2 | Staging GraphQL Introspection + Env Confusion | **VALID** | 6.5 | Report (bundle with #1) |
| 3 | Structural IDOR (userById/auth-omission) | **HOLD** | — | Needs test account / program consent |
| 4 | Staging testing_getConfirmationCode Auth Bypass | **HOLD** | — | Fold into #2 or wait for escalation |
| 5 | Relay Health/Metrics Disclosure | **INVALID** | — | Descriptive infra only; reject |
| 6 | JWT Algorithm Confusion | **HOLD** | — | Needs AUTH_HELPED; JWKS 404 |

**VALID leads ready for report: 2** (Leads 1+2). Recommend bundling into a single report covering production + staging GraphQL introspection with WAF bypass.
