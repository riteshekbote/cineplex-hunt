
===== TRIAGE 2026-09-12 01:34:53 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/*"[90m 100 matches[0m
[0m→ [0mRead .
I notice the LEADS section appears to be empty - no actual leads were provided for validation.

Let me check if there are relevant files in the repository that might contain leads or scope information:
[0m→ [0mRead new-leads.txt
[0m→ [0mRead probe-results.md
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/cineplex.md
Let me read the leads and probe results to find what needs to be triaged:
[0m• [0mRead all lead files[90m General Agent[0m
[0m→ [0mRead triage-output.txt
[0m→ [0mRead triage-prompt.txt
Let me read the individual lead files and recent triage runs to understand the full picture:
[0m✓ [0mRead all lead files[90m General Agent[0m
[0m→ [0mRead leads/lead-bigpickle.md[90m [limit=200][0m
[0m→ [0mRead leads/lead-nemotron3.md[90m [limit=200][0m
Let me read the actual lead findings from the most detailed files:
[0m→ [0mRead leads/lead-bigpickle.md[90m [offset=2800][0m
[0m→ [0mRead reports/valid-bugs.md
Let me read the final consolidated findings from the latest bigpickle cycle:
Now I have the full picture. Here is the complete triage against the 7-Question Gate for all distinct leads extracted from the hunt bot output.

---

## TRIAGE — Cineplex Deutschland GmbH & Co. KG

### LEAD 1: Production GraphQL Introspection Enabled (Full Schema Exposure)
**Asset:** `graphql-api.app.cineplex.de`
**Class:** MISCONFIG — GraphQL introspection enabled in production

| Gate | Answer |
|------|--------|
| Q1 In scope? | **YES** — `graphql-api.app.cineplex.de` is in `inventory/cineplex.md` (line 38); program scope covers "all company-owned infrastructure, APIs" |
| Q2 Reachable? | **YES** — publicly reachable; POST with introspection query returns 200 + full schema with zero auth |
| Q3 Real impact? | **YES** — full schema disclosure of 100+ mutations (`login`, `startBookingProcess`, `updateUserAdminStatus`, `deleteCineplexUser`, `buyVoucher`, `redeemVoucher`) + queries exposing PII (`userById`, `searchUsers`, `adminUsers`); enables targeted IDOR/BOLA and mutation attacks |
| Q4 Provable non-invasively? | **YES** — single read-only POST with `{"query":"{__schema{types{name fields{name}}}}"}` returns 200 + full schema; no auth, no data modification |
| Q5 Novel/unreported? | **YES** — WAF bypass (GET 403 / POST 200) is host-specific; no prior report found in triage history; confirmed active since 2026-09-03 |
| Q6 Not always-rejected? | **YES** — not on the program's out-of-scope list; GraphQL introspection is explicitly accepted in LEARN entries |
| Q7 Reasonable triager? | **YES** — production GraphQL introspection with WAF bypass exposing booking/payment/admin mutations is a valid misconfiguration |

**Verdict: VALID**

**Proof steps (read-only):**
1. `POST https://graphql-api.app.cineplex.de/` — `Content-Type: application/json` — body: `{"query":"{__schema{mutationType{fields{name}}}}"}` → 200, full mutation list
2. `POST` with `{"query":"{__schema{queryType{fields{name}}}}"}` → 200, full query list

**Impact:** Full API schema enumeration enabling targeted attacks on booking, payment, user admin, and voucher flows
**CVSS 3.1:** 5.3 (Medium) — CIA-only (no auth bypass, but schema enables follow-on attacks)
**Reporting channel:** bugs.olivermaicher.eu

---

### LEAD 2: Production Systemic IDOR via GraphQL Single-Entity Resolvers
**Asset:** `graphql-api.app.cineplex.de`
**Class:** IDOR — Auth-omission on `userById`, `invoice`, `order`, `ticket` resolvers

| Gate | Answer |
|------|--------|
| Q1 In scope? | **YES** — same asset as Lead 1; covered by program scope |
| Q2 Reachable? | **YES** — resolvers reachable via GET/POST with zero auth; return 200 (INVALID_ID) for `id:"0"` while 4 gate siblings (`searchUsers`, `adminUsers`, `userByQr`, `voucherInstanceByQR`) correctly return FORBIDDEN |
| Q3 Real impact? | **YES** — auth-omission on 4 resolvers that return full PII (email, fullName, telephone, birthDate, address, tickets, orders, subscriptions, invoices, vouchers); cross-tenant access = Critical |
| Q4 Provable non-invasively? | **PARTIAL** — structural proof complete (typed 200 INVALID_ID vs FORBIDDEN control group across both envs); cross-tenant PII fetch requires HUMAN_ONLY (two test accounts, program forbids PII exposure during testing) |
| Q5 Novel/unreported? | **YES** — systemic pattern across 4 resolvers with decodePublicId before gate; no prior report |
| Q6 Not always-rejected? | **YES** — IDOR/BOLA explicitly prioritized in program directives |
| Q7 Reasonable triager? | **HOLD** — structural proof is strong (control-complete: 4 omissions vs 4 firing gates), but strict triager will demand at least one valid cross-user PII fetch before accepting Critical severity |

**Verdict: HOLD**

**Reason:** Structural POC complete but cross-tenant PII proof (HUMAN_ONLY) required for full acceptance. Reopen with two-account demonstration showing 200 + real PII for tenant B's ID.

---

### LEAD 3: Staging Auth Bypass — `testing_getConfirmationCode` Missing Environment Guard
**Asset:** `graphql-api.app.staging.cineplex.de`
**Class:** AUTH — Missing env guard on staging-only testing mutation

| Gate | Answer |
|------|--------|
| Q1 In scope? | **YES** — `graphql-api.app.staging.cineplex.de` is in `inventory/cineplex.md` (line 40); staging explicitly in scope |
| Q2 Reachable? | **YES** — authless GET returns 200 (backend hit, 405-mismatch on `/userPasswordResets/...`) while production returns FORBIDDEN; persisted 8+ cycles |
| Q3 Real impact? | **YES** — confirmation code oracle → password reset → ATO; `forceDeleteUser` mutation also exposed → mass deletion |
| Q4 Provable non-invasively? | **PARTIAL** — GET-based evidence (200 vs prod FORBIDDEN) confirms missing guard; full proof requires POST mutation returning actual code (HUMAN_ONLY) |
| Q5 Novel/unreported? | **YES** — testing-only mutation left accessible on staging with prod-parity schema; no prior report |
| Q6 Not always-rejected? | **YES** — staging env confusion is an accepted finding class in LEARN entries |
| Q7 Reasonable triager? | **HOLD** — evidence is compelling (8-cycle persistence, env-parity proven) but final proof requires POST demonstrating code extraction → ATO chain |

**Verdict: HOLD**

**Reason:** Structural proof strong but POST code-extraction (HUMAN_ONLY) needed for full ATO chain demonstration.

---

### LEAD 4: Staging Internal Architecture Leak via Introspection
**Asset:** `graphql-api.app.staging.cineplex.de`
**Class:** MISCONFIG — Spring Data JPA REST endpoints disclosed in schema

| Gate | Answer |
|------|--------|
| Q1 In scope? | **YES** — same staging asset |
| Q2 Reachable? | **YES** — endpoints disclosed via GraphQL introspection (schema-level); HTTP GET probes return 403 (WAF-blocked) |
| Q3 Real impact? | **MARGINAL** — Spring Data JPA REST paths, `mandatorId` UUID, service name `LOGIN`, Lambda path disclosed; informational only, no direct exploitation path |
| Q4 Provable non-invasively? | **YES** — introspection query returns schema with these endpoints; but HTTP GET returns 403 (not directly reachable) |
| Q5 Novel/unreported? | **MARGINAL** — architecture disclosure via introspection is a secondary effect of Lead 3; not independently novel |
| Q6 Not always-rejected? | **YES** — "Descriptive error messages or headers" is out of scope, but this is schema-level disclosure, not error messages |
| Q7 Reasonable triager? | **MAYBE** — would be folded into staging env confusion report as supporting evidence; not independently reportable |

**Verdict: HOLD** (fold into Lead 3 report as supporting evidence)

---

### LEAD 5: WAF Method-Gate Attenuation (GET 403 / POST 200)
**Asset:** `graphql-api.app.{,staging.}cineplex.de`
**Class:** MISCONFIG — Client-differentiated bot-gate

| Gate | Answer |
|------|--------|
| Q1 In scope? | **YES** |
| Q2 Reachable? | **YES** — GET returns 403 (Cloudflare bot-gate), POST returns 200; balanced URL-encoded GET `?query=%7B__typename%7D` → 200 origin |
| Q3 Real impact? | **NO** — this is the WAF working as designed (bot-differentiation); it's a security control, not a vulnerability |
| Q4 Provable non-invasively? | N/A |
| Q5 Novel/unreported? | N/A |
| Q6 Not always-rejected? | N/A — informational observation only |
| Q7 Reasonable triager? | **NO** — WAF bot-gate attenuation is expected behavior; not a finding |

**Verdict: INVALID** — informational observation, not a vulnerability; included in report as context for Leads 1-2 (how the WAF bypass was discovered)

---

### LEAD 6: Relay Health/Build Endpoint Disclosure
**Asset:** `data-9fc27eb430.cineplex.de`
**Class:** MISCONFIG — Exposed health/metrics/build header

| Gate | Answer |
|------|--------|
| Q1 In scope? | **YES** — in `inventory/cineplex.md` (line 30); Cineplex-owned |
| Q2 Reachable? | **YES** — `/health` returns `{"status":"ok"}` (200, no auth); `/metrics` returns IOMB broker stats (200, ~115 bytes) |
| Q3 Real impact? | **NO** — `/health` returns only `{"status":"ok"}`; `/metrics` returns descriptive IOMB broker stats (queue length, capacity, messages queued/dropped); build header `X-Powered-By: cST-...` is descriptive only; no PII, no sensitive data, no exploitation path |
| Q4 Provable non-invasively? | N/A |
| Q5 Novel/unreported? | N/A |
| Q6 Not always-rejected? | Program scope excludes "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)"; `X-Powered-By` header falls here; "Disclosure of known public files or directories" is also excluded |
| Q7 Reasonable triager? | **NO** — informational infrastructure disclosure without exploitable surface |

**Verdict: INVALID** — falls under excluded "descriptive headers" and "known public files"; no exploitable surface

---

## SUMMARY

| # | Lead | Verdict | CVSS | Reporting |
|---|------|---------|------|-----------|
| 1 | Production GraphQL Introspection (schema exposure + WAF bypass) | **VALID** | 5.3 | bugs.olivermaicher.eu |
| 2 | Production Systemic IDOR (4 resolvers, auth-omission) | **HOLD** | — | Needs cross-tenant PII proof |
| 3 | Staging Auth Bypass (`testing_getConfirmationCode`) | **HOLD** | — | Needs POST code-extraction proof |
| 4 | Staging Architecture Leak (Spring JPA REST) | **HOLD** | — | Fold into #3 |
| 5 | WAF Method-Gate Attenuation | **INVALID** | — | Informational only |
| 6 | Relay Health/Metrics Disclosure | **INVALID** | — | Descriptive headers, out of scope |

**Actionable now:** Lead 1 ready for submission. Leads 2-3 require HUMAN_ONLY testing sessions with two test accounts to complete POC.
