
===== TRIAGE 2026-09-13 23:14:16 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/inventory/**"[90m 1 match[0m
I see the LEADS section is empty. Let me first check the repository for program scope and related files.
[0m✱ [0mGlob "**/scope*"[90m 1 match[0m
[0m✱ [0mGlob "**/*.{yml,yaml,md}"[90m 96 matches[0m
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/cineplex.md
[0m→ [0mRead probe-results.md
Let me read the scope, inventory, and probe results to understand the context.
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/reposcan-cineplex.md
Let me read the lead files and the latest triage run for context.
Let me check the latest triage run and valid bugs report for context on what's already been triaged.
[0m→ [0mRead triage/run-2026-09-13-21-15.md
[0m→ [0mRead reports/valid-bugs.md
The LEADS section in your message is empty. However, I've read all lead files and the full probe history. Let me compile the distinct findings from across all hunt bot models and triage them against the 7-Question Gate.

---

## TRIAGE — 2026-09-13 21:15 UTC

### LEAD 1: Production GraphQL Introspection via POST (WAF bypass)
**Asset:** `graphql-api.app.cineplex.de` | **Source:** bigpickle, nemotron3

| Q | Gate | Verdict |
|---|------|---------|
| Q1 Scope? | **YES** — Cineplex-owned API domain under `*.cineplex.de` scope | ✅ |
| Q2 Reachable? | **YES** — anonymous POST returns 200 with full schema; GET returns 403 (method-gate WAF bypass) | ✅ |
| Q3 Impact? | **YES** — full schema disclosure (200+ types, 100+ mutations including `login`, `startBookingProcess`, `updateUserAdminStatus`, `deleteCineplexUser`) enables targeted IDOR/BOLA/mutation attacks | ✅ |
| Q4 Provable? | **YES** — `POST https://graphql-api.app.cineplex.de/` with `Content-Type: application/json` body `{"query":"{__schema{types{name}}}"}` returns 200 + JSON; probe-results.md confirms GET=403, prior KB confirms POST=200 | ✅ |
| Q5 Novel? | **YES** — confirmed via KB since 2026-09-03; no prior triage rejection on file | ✅ |
| Q6 Rejected list? | **NO** — not on always-rejected; "Descriptive error messages" out-of-scope but this is schema disclosure, not error messages | ✅ |
| Q7 Reasonable triager? | **YES** — GraphQL introspection enabled in production with WAF method-gate bypass is a valid misconfiguration; prior triages at 09-04, 09-06, 09-07, 09-10, 09-11, 09-12, 09-13 all marked VALID | ✅ |

**Verdict: VALID**

**Proof steps:** POST `https://graphql-api.app.cineplex.de/` — `Content-Type: application/json` — body: `{"query":"{__schema{queryType{name} mutationType{name} types{name}}}"}` — returns 200 with full schema JSON. Single read-only request, ≤1 rps.

**Impact:** Full API schema enumeration → targeted mutation/IDOR attacks → potential ATO, PII disclosure, booking fraud. **CVSS 3.1: 7.5 (High)** — AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N (schema-only disclosure).

**Report channel:** `bugs.olivermaicher.eu`

---

### LEAD 2: Staging GraphQL Introspection + Environment Confusion
**Asset:** `graphql-api.app.staging.cineplex.de` | **Source:** bigpickle, nemotron3

| Q | Gate | Verdict |
|---|------|---------|
| Q1 Scope? | **YES** — staging subdomain under `*.cineplex.de` | ✅ |
| Q2 Reachable? | **YES** — same WAF bypass as prod (GET 403/POST 200); KB confirms introspection returns 140 mutations vs prod's 100+ | ✅ |
| Q3 Impact? | **YES** — staging with identical or expanded schema exposed to public; env confusion = easier exploitation path; staging has additional `testing_getConfirmationCode` mutation that resolves with zero auth | ✅ |
| Q4 Provable? | **YES** — POST introspection query same as Lead 1; KB confirms 200 + full schema on staging | ✅ |
| Q5 Novel? | **YES** — not on any rejected list; staging env confusion is a distinct finding class | ✅ |
| Q6 Rejected list? | **NO** | ✅ |
| Q7 Reasonable triager? | **YES** — staging environment exposed to internet with same or expanded production schema is valid; prior triages at 09-04, 09-06, 09-07, 09-10, 09-11, 09-12, 09-13 all marked VALID | ✅ |

**Verdict: VALID**

**Proof steps:** POST `https://graphql-api.app.staging.cineplex.de/` — `Content-Type: application/json` — body: `{"query":"{__schema{queryType{name} mutationType{name}}}"}` — returns 200 with staging schema (140 mutations including `testing_getConfirmationCode`). Read-only, ≤1 rps.

**Impact:** Staging env confusion → same attack surface as prod with potentially weaker auth; env marker (`testing_` mutations) confirms non-production data at risk. **CVSS 3.1: 6.5 (Medium)** — AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N.

**Report channel:** `bugs.olivermaicher.eu` (bundle with Lead 1)

---

### LEAD 3: Relay Host Internal Disclosure (/health + /metrics)
**Asset:** `data-9fc27eb430.cineplex.de` | **Source:** bigpickle

| Q | Gate | Verdict |
|---|------|---------|
| Q1 Scope? | **YES** — `data-9fc27eb430.cineplex.de` is Cineplex-owned (CNAME: `cineplex-relay.iocnt.net`) | ✅ |
| Q2 Reachable? | **YES** — unauthenticated GET /health returns 200 `{"status":"ok"}`, /metrics returns 200 with IOMB broker stats | ✅ |
| Q3 Impact? | **MARGINAL** — `/health` is `{"status":"ok"}` (15 bytes), `/metrics` is IOMB broker stats (115 bytes, no PII/sensitive data). Build header (`cST-...-prd`) changes across cycles confirming live CI/CD, but descriptive infra only. No SSRF forwarding primitive confirmed. | ⚠️ |
| Q4 Provable? | **YES** — GET endpoints return 200; probe-results.md confirms | ✅ |
| Q5 Novel? | **YES** — butKB notes this as "descriptive infra only, no PII" and "NOT reportable alone per KB" | ⚠️ |
| Q6 Rejected list? | **NO** — but "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)" is explicitly out of scope; /metrics + build header is arguably in this class | ❌ |
| Q7 Reasonable triager? | **NO** — `/health` = `{"status":"ok"}` and `/metrics` = IOMB broker queue stats (queue_length, queue_capacity, messages_queued, messages_dropped) are descriptive infra metrics with no exploitable security impact. No PII, no auth bypass, no SSRF confirmed. A reasonable triager would reject this as informational-only. | ❌ |

**Verdict: INVALID**

**Reason:** `/health` and `/metrics` expose only descriptive infrastructure metrics (queue stats, build headers) with no security impact. Falls under "Descriptive error messages or headers" out-of-scope clause. No exploitable primitive confirmed (no SSRF forwarding, no auth bypass, no PII).

---

### LEAD 4: JWT Algorithm Confusion on Central Auth
**Asset:** `auth.cineplex.de` | **Source:** bigpickle, nemotron3

| Q | Gate | Verdict |
|---|------|---------|
| Q1 Scope? | **YES** — `auth.cineplex.de` is in-scope | ✅ |
| Q2 Reachable? | **PARTIAL** — endpoint exists but `/well-known/jwks.json` returns 404 (no passive key fetch) | ⚠️ |
| Q3 Impact? | **YES (if confirmed)** — token forgery → ATO across all Cineplex properties | ✅ |
| Q4 Provable? | **NO** — JWKS 404 blocks passive verification; requires valid credentials to login via GraphQL mutation, capture JWT, decode header, forge HS256, replay. AUTH_HELPED required. Program forbids exposing customer PII during testing. | ❌ |
| Q5 Novel? | **YES** — but cannot verify without credentials | ⚠️ |
| Q6 Rejected list? | **NO** — JWT alg confusion is explicitly prioritized | ✅ |
| Q7 Reasonable triager? | **HOLD** — strong hypothesis but no proof; JWKS 404 blocks passive verification; needs test account or program consent | ⚠️ |

**Verdict: HOLD**

**Reason:** JWKS endpoint returns 404 — no passive verification possible. Requires AUTH_HELPED session with test credentials. Reopen when test account available or program provides consent.

---

### LEAD 5: IDOR via GraphQL userById/searchUsers/adminUsers
**Asset:** `graphql-api.app.cineplex.de` | **Source:** bigpickle, nemotron3

| Q | Gate | Verdict |
|---|------|---------|
| Q1 Scope? | **YES** | ✅ |
| Q2 Reachable? | **PARTIAL** — GraphQL endpoint reachable; resolvers confirmed in schema; but cross-user query requires valid JWT | ⚠️ |
| Q3 Impact? | **YES (if confirmed)** — full PII dump (email, phone, address, tickets, orders, subscriptions, invoices, vouchers) | ✅ |
| Q4 Provable? | **NO** — requires valid JWT + querying other users' data = PII exposure, which program explicitly forbids during testing | ❌ |
| Q5 Novel? | **YES** — but cannot verify without violating program rules | ⚠️ |
| Q6 Rejected list? | **NO** | ✅ |
| Q7 Reasonable triager? | **HOLD** — structural proof from schema analysis is strong (4 auth-omission resolvers vs 4 firing-gate resolvers), but strict triager demands at least one valid cross-user PII fetch. Program forbids this without test/sandbox account. | ⚠️ |

**Verdict: HOLD**

**Reason:** IDOR via `userById`/`searchUsers`/`adminUsers` requires valid JWT + cross-user PII query = PII exposure during testing, explicitly forbidden by program. Reopen with test/sandbox account or explicit program consent.

---

### LEAD 6: Mutation Authorization Gap (Admin Mutations via Anonymous POST)
**Asset:** `graphql-api.app.cineplex.de` | **Source:** bigpickle

| Q | Gate | Verdict |
|---|------|---------|
| Q1 Scope? | **YES** | ✅ |
| Q2 Reachable? | **PARTIAL** — anonymous POST reaches Mutation root; admin mutations visible in schema | ⚠️ |
| Q3 Impact? | **YES (if confirmed)** — mass PII exposure, voucher minting, ticket cancellation, admin escalation | ✅ |
| Q4 Provable? | **NO** — calling admin mutation without token likely returns UNAUTHENTICATED; verifying requires executing mutations on live data = program violation | ❌ |
| Q5 Novel? | **YES** — but cannot verify without live mutation execution | ⚠️ |
| Q6 Rejected list? | **NO** | ✅ |
| Q7 Reasonable triager? | **HOLD** — mutation auth gap hypothesis is strong from schema analysis, but proof requires executing mutations on live customer data (forbidden) or test account | ⚠️ |

**Verdict: HOLD**

**Reason:** Admin mutation auth gap requires live mutation execution to verify; program forbids modifying live customer data. Reopen with sandbox/test account.

---

### LEAD 7: Relay SSRF/Proxy Primitive
**Asset:** `data-9fc27eb430.cineplex.de` | **Source:** bigpickle, nemotron3

| Q | Gate | Verdict |
|---|------|---------|
| Q1 Scope? | **YES** | ✅ |
| Q2 Reachable? | **YES** — /health, /metrics, root all return 200 | ✅ |
| Q3 Impact? | **MARGINAL** — CNAME `cineplex-relay.iocnt.net` suggests relay function, but no forwarding/SSRF primitive has been confirmed; all common relay paths (/proxy, /forward, /api, /admin) return 404 | ⚠️ |
| Q4 Provable? | **NO** — no SSRF-forwarding behavior observed in probe-results; hypothesis remains unconfirmed | ❌ |
| Q5 Novel? | **YES** — but hypothesis only | ⚠️ |
| Q6 Rejected list? | **NO** | ✅ |
| Q7 Reasonable triager? | **NO** — speculative SSRF with no evidence; all probe paths returned 404; relay may just be a health-check endpoint, not a forwarding proxy. No reasonable triager accepts SSRF without a confirmed forwarding primitive. | ❌ |

**Verdict: INVALID**

**Reason:** Speculative SSRF hypothesis with no confirmed forwarding primitive. All common relay paths return 404. No evidence of URL reflection or upstream proxying.

---

## SUMMARY

| # | Lead | Verdict | CVSS 3.1 | Channel |
|---|------|---------|----------|---------|
| 1 | Prod GraphQL Introspection (WAF bypass) | **VALID** | 7.5 High | bugs.olivermaicher.eu |
| 2 | Staging GraphQL Introspection + Env Confusion | **VALID** | 6.5 Medium | bugs.olivermaicher.eu (bundle w/ #1) |
| 3 | Relay Host Internal Disclosure | **INVALID** | — | Descriptive infra, out-of-scope |
| 4 | JWT Alg Confusion | **HOLD** | — | JWKS 404; needs AUTH_HELPED |
| 5 | IDOR via GraphQL userById/searchUsers | **HOLD** | — | Needs test account; PII exposure forbidden |
| 6 | Mutation Authorization Gap | **HOLD** | — | Needs sandbox; live mutations forbidden |
| 7 | Relay SSRF/Proxy Primitive | **INVALID** | — | No confirmed forwarding; speculative |

**Valid leads ready for report: 2** (Leads 1+2). Recommend bundling into a single report covering production + staging GraphQL introspection with WAF method-gate bypass (GET 403 / POST 200).
