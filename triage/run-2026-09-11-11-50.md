
===== TRIAGE 2026-09-11 11:50:08 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/*.yml"[90m 1 match[0m
[0m✱ [0mGlob "**/{scope.yml,inventory/cineplex.md}"[90m 2 matches[0m
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/cineplex.md
[0m✱ [0mGlob "**/lead*"[90m 6 matches[0m
[0m✱ [0mGlob "**/*lead*"[90m 7 matches[0m
[0m→ [0mRead inventory/cineplex.md[90m [offset=566][0m
Let me check for any leads files in the repo.
[0m→ [0mRead new-leads.txt
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-mimo.md
**No leads provided in the prompt.** The `LEADS:` section is empty.

I've read all 6 lead files and the inventory. The only models producing substantive hypotheses are **bigpickle** and **nemotron3**. The other 4 (ling3, longcat, laguna, mimo) produced empty timestamp-only entries with zero hypotheses.

I'll triage the **distinct hypotheses** across both productive lead files:

---

## LEAD 1: Production GraphQL Introspection — `graphql-api.app.cineplex.de`

| Gate | Answer |
|---|---|
| Q1 Scope | YES — `*.cineplex.de` company-owned infrastructure |
| Q2 Reachable | YES — unauthenticated POST returns 200 with full schema |
| Q3 Impact | YES — full schema disclosure (200+ types, 100+ mutations including login, booking, admin) enables targeted IDOR/BOLA/mutation attacks |
| Q4 Provable | YES — single `curl -X POST` with introspection query, read-only |
| Q5 Novel | YES — first report, not on known lists for this program |
| Q6 Not rejected | YES — not on always-rejected list (introspection ≠ descriptive errors/banner grabbing) |
| Q7 Reasonable triager | YES — GraphQL introspection on production is a well-accepted finding class |

**Verdict: VALID**
- **Proof:** `POST https://graphql-api.app.cineplex.de/` with `{"query":"{__schema{queryType{name fields{name}}}}"}` → 200 + full schema. Automated urllib gets 403 (WAF bot-gate); `curl --http2` passes (client-differentiated).
- **Impact:** Full API enumeration → targeted exploitation of mutations/queries; medium (schema-only, no data exfiltration without auth).
- **CVSS 3.1:** 5.3 (AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N)
- **Report via:** bugs.olivermaicher.eu

---

## LEAD 2: Staging GraphQL Environment Confusion — `graphql-api.app.staging.cineplex.de`

| Gate | Answer |
|---|---|
| Q1 Scope | YES |
| Q2 Reachable | YES — unauthenticated POST returns 200 with full schema (140 mutations, 83 queries) |
| Q3 Impact | YES — staging has additional mutations not in prod; `testing_getConfirmationCode` resolves authless (200, backend hit) vs prod FORBIDDEN |
| Q4 Provable | YES — `curl -X POST` with introspection query + `testing_getConfirmationCode` probe |
| Q5 Novel | YES — staging env confusion is a real misconfiguration |
| Q6 Not rejected | YES |
| Q7 Reasonable triager | YES — staging exposing additional mutation surface with missing environment guards is reportable |

**Verdict: VALID**
- **Proof:** POST introspection → 200 (staging schema has 140 mutations vs prod). `testing_getConfirmationCode` → 200 with backend hit vs prod FORBIDDEN. Spring Data JPA REST endpoints disclosed in schema (`userPasswordResets`, `userRegistrations`, `mandatorId` UUID, service name `LOGIN`, Lambda path, Apollo stacktrace).
- **Impact:** Staging bypass → test exploit paths against weaker-gated mutations; medium-high.
- **CVSS 3.1:** 6.5 (AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:L/A:N)
- **Report via:** bugs.olivermaicher.eu

---

## LEAD 3: Structural Auth-Omission IDOR via GraphQL `userById`/`invoice`/`order`/`ticket` — `graphql-api.app.cineplex.de`

| Gate | Answer |
|---|---|
| Q1 Scope | YES |
| Q2 Reachable | YES — GET-based queries return 200 with no Authorization header |
| Q3 Impact | YES — `userById(id:"0")` → 200 `INVALID_ID` with `decodePublicId` stacktrace (typed error, resolver processed the ID) vs `currentUser` → 200 `UNAUTHENTICATED` on same surface — proves auth gate exists on sibling but is omitted on 4 resolvers |
| Q4 Provable | YES — single `curl` GET with no auth, read-only, no customer PII fetched |
| Q5 Novel | YES — systemic auth-omission across 4 resolvers is not a trivial config issue |
| Q6 Not rejected | YES — IDOR/BOLA is explicitly accepted in directives |
| Q7 Reasonable triager | MAYBE — the proof is structural (typed error confirms resolver processed ID without auth) but no real user record was fetched. A strict triager may demand at least one valid-ID fetch returning data. However, the asymmetry with `currentUser` + typed error path is strong structural evidence. |

**Verdict: HOLD**
- **Reason:** Strong structural proof of auth-omission, but no real PII returned. Program forbids exposing customer data during testing. Needs either (a) a test/sandbox account to fetch own record, or (b) explicit program consent for a single cross-ID fetch to confirm exploitability.
- **If confirmed:** CVSS 3.1: 8.1 (AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) — cross-tenant PII access
- **Report via:** bugs.olivermaicher.eu

---

## LEAD 4: Mutation Authorization Gap — Admin Mutations Exposed via Anonymous POST — `graphql-api.app.cineplex.de`

| Gate | Answer |
|---|---|
| Q1 Scope | YES |
| Q2 Reachable | YES — anonymous POST reaches Mutation root |
| Q3 Impact | YES — `updateUserAdminStatus`, `deleteCineplexUser`, `sendNotifications`, `createVoucherInstances`, `cancelTicket` are admin-class mutations |
| Q4 Provable | PARTIALLY — static schema analysis shows no auth handle vs `currentUser` gate, but calling mutations on live data = OUT OF SCOPE per program rules |
| Q5 Novel | YES |
| Q6 Not rejected | YES |
| Q7 Reasonable triager | NO — calling mutations on live customer data is explicitly forbidden; static schema analysis alone is insufficient proof of exploitability |

**Verdict: INVALID**
- **Reason:** Requires executing mutations on live customer data to prove, which the program explicitly forbids. Schema-only analysis shows the gap but doesn't prove the mutation succeeds without auth.

---

## LEAD 5: JWT Algorithm Confusion — `auth.cineplex.de` / `api.cineplex.de`

| Gate | Answer |
|---|---|
| Q1 Scope | YES |
| Q2 Reachable | PARTIALLY — `auth.cineplex.de/.well-known/jwks.json` returns 404; `api.cineplex.de` returns 403 on all paths |
| Q3 Impact | YES — if confirmed, token forgery → full ATO |
| Q4 Provable | NO — no token obtainable, no JWKS endpoint, no way to verify passively |
| Q5 Novel | YES (hypothesis) |
| Q6 Not rejected | YES — JWT alg confusion explicitly prioritized in directives |
| Q7 Reasonable triager | NO — zero evidence, pure hypothesis |

**Verdict: INVALID**
- **Reason:** Zero passive evidence obtainable. JWKS 404, API 403-gated, no token acquisition path. Pure hypothesis with no verification possible.

---

## LEAD 6: Undocumented Relay API / SSRF Primitive — `data-9fc27eb430.cineplex.de`

| Gate | Answer |
|---|---|
| Q1 Scope | YES |
| Q2 Reachable | YES — 200 on `/health`, `/metrics`, `/` |
| Q3 Impact | SPECULATIVE — no evidence of forwarding/proxy behavior; all `/api*`, `/proxy*`, `/admin*` paths return 404 |
| Q4 Provable | NO — no path reflecting caller-controlled input discovered |
| Q5 Novel | N/A |
| Q6 Not rejected | YES |
| Q7 Reasonable triager | NO — no evidence of SSRF primitive exists; relay is a message broker (IOMB), not a forwarder |

**Verdict: INVALID**
- **Reason:** No SSRF evidence. All path enumeration returned 404. The relay is a message queue broker (IOMB), not an HTTP forwarder. Speculation without proof.

---

## LEAD 7: Relay Metrics Disclosure — `data-9fc27eb430.cineplex.de/metrics`

| Gate | Answer |
|---|---|
| Q1 Scope | YES |
| Q2 Reachable | YES — 200 with IOMB broker stats |
| Q3 Impact | NO — descriptive infra only (queue_length, messages_queued, messages_dropped); no PII, no sensitive data, no auth bypass |
| Q4 Provable | YES |
| Q5 Novel | NO |
| Q6 Not rejected | Borderline — falls under "Descriptive error messages or headers" spirit; KB explicitly marks `grafana/metrics-only` as REJECTED |
| Q7 Reasonable triager | NO |

**Verdict: INVALID**
- **Reason:** Descriptive infra metrics only. Program rejects "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)." KB explicitly rejects `grafana/metrics-only` class.

---

## LEAD 8: Nextcloud OCS Capabilities Exposed — `cloud.systems.cineplex.de`

| Gate | Answer |
|---|---|
| Q1 Scope | YES — `cloud.systems.cineplex.de` company-owned infrastructure |
| Q2 Reachable | YES — Nextcloud 33.0.8 "Cineplex-Cloud", OCS capabilities fully exposed unauthenticated, `/public.php` returns 500, brute-force delay=0, Talk/SIP federation disclosed |
| Q3 Impact | LOW — OCS capabilities exposure is information disclosure; brute-force delay=0 is on the always-rejected list ("Enforcement policies for brute force, rate limiting, or account lockout"); `/public.php` 500 is a server error, not a vulnerability |
| Q4 Provable | YES |
| Q5 Novel | NO — Nextcloud OCS exposure is a known/expected misconfiguration |
| Q6 Not rejected | PARTIALLY — brute-force policy is explicitly out of scope; OCS capabilities disclosure may fall under descriptive headers |
| Q7 Reasonable triager | NO — brute-force enforcement is explicitly out of scope; OCS capabilities alone is a low-value finding |

**Verdict: INVALID**
- **Reason:** Brute-force delay=0 is explicitly out of scope. OCS capabilities disclosure is descriptive/known. No exploitable impact demonstrated.

---

## Summary

| # | Lead | Verdict | CVSS |
|---|---|---|---|
| 1 | GraphQL introspection (prod) | **VALID** | 5.3 |
| 2 | Staging GraphQL env confusion | **VALID** | 6.5 |
| 3 | Structural auth-omission IDOR | **HOLD** | 8.1 (if confirmed) |
| 4 | Mutation auth gap | **INVALID** | — |
| 5 | JWT alg confusion | **INVALID** | — |
| 6 | Relay SSRF primitive | **INVALID** | — |
| 7 | Relay metrics disclosure | **INVALID** | — |
| 8 | Nextcloud OCS exposure | **INVALID** | — |

**Actionable reports:** Leads 1 and 2 are ready to submit to bugs.olivermaicher.eu. Lead 3 needs a test account or program consent before it can be reported.
